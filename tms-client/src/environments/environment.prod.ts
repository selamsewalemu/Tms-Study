export const environment = {
  production: true,
  // Relative URLs work on Render because the .NET API serves the Angular
  // SPA from wwwroot — both the API and the SPA are on the same origin.
  apiUrl: "/api",
};
